# Environment variables (contains credentials — do NOT commit)
.env

# SQLite database and WAL files
data/
*.db
*.db-shm
*.db-wal

# Build artifacts (binaries at repo root only)
/ai-news
/server

# macOS
.DS_Store

# Editors
.idea/
.vscode/
*.swp
*.swo

# Test cache
vendor/
